'use client'

// StageOne is now integrated into the chat-first interface.
// This file is kept as a no-op export to prevent import errors if referenced elsewhere.

export default function StageOne() {
  return null
}
